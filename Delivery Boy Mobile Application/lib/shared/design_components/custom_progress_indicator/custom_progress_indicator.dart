//TODO: Build Custom Circular Progress Indicator Here

import 'package:flutter/material.dart';

class CustomCircullarProgressIndicator extends CircularProgressIndicator {
  const CustomCircullarProgressIndicator({Key key}) : super(key: key);

  Widget build(BuildContext context) {
    return CircularProgressIndicator();
  }
}
